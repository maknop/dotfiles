name: Fish Lint

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  fish-syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install fish
        run: sudo apt-get install -y fish

      - name: Check fish syntax
        run: make fish-lint
